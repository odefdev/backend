
namespace Service.Services
{
    public  interface IGenericService<TEntity>
    {
      //  BaseResponse<TEntity> Get(int id);
        //IEnumerable<T> GetAll();
        //IEnumerable<T> Find(Expression<Func<TEntity, bool>> predicate);


 
        //T AddRange(IEnumerable<TEntity> entities);

        //T Remove(TEntity entity);
        //T RemoveRange(IEnumerable<TEntity> entities);

    }
}
